# 安全策略

本项目把用户提供的 PE 文件视为不可信数据。仓库启用 GitHub Security Advisories 后，请通过私密渠道报告漏洞；不要在公开 issue 中披露利用细节或凭据。

CLI 仅本地离线运行，并限制 PE 大小、图标字节/尺寸/像素和 VERSIONINFO 字符串；畸形输入会被拒绝且不执行写入。工具可以检测证书表，但不进行密码学验证。资源修改可能使签名失效，必须双重显式确认。

支持范围以当前 Draft 文档为准，不承诺 SVG/PDF/MCP/UI 或 Windows Explorer 验证。报告时请提供 commit、平台、命令、输入类别、影响和最小复现，不要上传私有二进制或秘密。
