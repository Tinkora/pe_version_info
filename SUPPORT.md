# Support

This is a Draft local CLI/library. Before opening an issue, run the locked Rust checks, capture the exact command, OS, Rust version, `pevi --version`, stable error code, and a minimal reproducible fixture description. Do not upload private executables or secrets.

Use GitHub Issues for reproducible defects and documentation questions. Use the private security channel described in `SECURITY.md` for vulnerabilities.
